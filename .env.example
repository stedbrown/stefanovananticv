# Chiave API OpenAI
# Ottieni la tua chiave API da https://platform.openai.com/api-keys
# IMPORTANTE: In un ambiente di produzione, questa chiave dovrebbe essere gestita da un backend
# e non esposta nel frontend. Questa implementazione è solo per scopi dimostrativi.
VITE_OPENAI_API_KEY=la_tua_chiave_api_qui

# Configurazione di build
# Imposta su 'production' per la build di produzione
NODE_ENV=development